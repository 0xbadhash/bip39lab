# CROSS-REVIEW

Working tree vs origin/master. V2 only.

## Security Guru

**none (blocker).** Offline CSP unchanged. No mnemonic in sessionStorage. Practice balances are copy. Passphrase estimate is Shannon/charset teaching-only, capped 256, not claimed as wallet ENT.

## Maintainability Expert

**none (blocker).** Tracks 14–15 share pad helpers (`entHeroHtml`, `mintFromPad`, lock tint). Classic Lab stills reused by URL, not copied.

### Obsolete / cleanup (scoped)

- **Tier A:** 0. No deletes.
- **Tier B:** `web/v2/js/lab-strip.js` 404 on `/v2/` — follow-up, not introduced as a new contract.
- **False keep:** dual stamp 0.16.x vs 0.17.N-v2; leftover scripts stash.

## Domain Specialist

**none (blocker).** 12-word 128 / 24-word 256 matches BIP-39 ENT. d6 ≈ 2.58, coin = 1 bit. Word count is not pad entropy. PP does not add into ENT. UC11 one-signer vs 2-of-3 is the right split.

## Severity counts

blocker 0 · major 0 · nit 0 (P0-only report)

## §9 intentional oddities

1. Fake 0.184 BTC.
2. Lock colour is hue-rotate on a blue PNG (red/green gradient).
3. UC15 passphrase bits are not added to pad bits.
4. lab-strip 404 on V2.
5. Dual product/V2 stamps.

# CODE-REVIEW

- **command:** `/code_review` vs origin/master (working tree → feat 0.16.41)
- **secrets:** gitleaks origin/master...HEAD (re-run after commit)
- **engine:** same session as implementer

## Accepted P0

**none**

Clear secrets is one topbar control (no duplicate IDs). Network select only changes local derive (CSP still connect-src none). Practice addresses; no fund path.

## Follow-ups

- leftover `scripts/*.py` uncommitted
- lab-strip 404 on `/v2/`
- dice/coin UC spec still draft (grill incomplete) — not this ship

p0=0 follow_ups=3

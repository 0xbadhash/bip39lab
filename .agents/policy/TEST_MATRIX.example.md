# Test matrix example — copy to product `.agents/policy/TEST_MATRIX.md`

| id | surface | must_exist (paths, comma-separated, relative to repo root) |
|----|---------|------------------------------------------------------------|
| unit-smoke | Core unit tests dir | tests/ |

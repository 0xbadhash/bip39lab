# Ship-chain skills that MUST be present after install_into_product.sh
# One skill directory name per line. Comments start with #.
#
# Required = installed + verify_skills. Router must route or honestly skip.
# qa_campaign / retrospect / audit_harness → optional_skills (not every ship).
spec
execute_dev
code_review
cross_review
behavior_validator
pr_review
release_mgmt
sync_docs
plan_review
sweep
night_shift
handoff
#
# plan_review: NEXT_SKILL after /spec when **Plan:** is linked; else skipped.
# qa_campaign: optional — only suggested after sync_docs on *large* ships (--force-qa).

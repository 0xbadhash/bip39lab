# Plan: V2 UC11–UC13 custody taxonomy

**Spec:** `.agents/specs/2026-08-24-v2-uc11-uc13.md`

## Approach

Reuse TRACKS / stepsFor / conceptsFor / conceptTarget / VIZ / `stepHtml` / `ucN` pad functions. Teaching copy: full sentences, no contractions. Blue `callout("done")` for the lock phrase. Quizzes: one `ok` + two `bad` with `why` starting “Wrong.”

## Architecture

- Assets: `web/v2/assets/uc11-atom-*.svg` … `uc13-atom-*.svg` (App Shell tokens).
- `uc11` they-hold / you-hold; `uc12` hot phone / hardware; `uc13` hot-cold / daily-savings.
- Boot deeplink `n <= 13`.

## Implementation

1. TRACKS 11–13, steps, chips, conceptTarget, VIZ catalog, nine SVGs.
2. `uc11` / `uc12` / `uc13` pad HTML (Do/Do not, desc, done callout, quiz, finish).
3. Boot `openUc` range, sidebar UC1–UC13, V2 `VERSION` + chip + `?v=0.17.22-v2`.
4. e2e V2-S0 count 13, S13 loop through 13, V2-S14 pads/quiz/exit; Comet S-id index.

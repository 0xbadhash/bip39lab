# Plan: V2 picker visual

**Spec:** `.agents/specs/2026-08-25-v2-picker-visual.md`

## Approach

App Shell tokens only. Path groups in `v2-app.js`. Skeleton in `index.html`. CSS for hero/dots/ghosts/filters.

## Implementation

1. Path picker + skeleton + Hard refresh + lock/copy nits.
2. Hero atoms, step index, dots, ghost Keys teasers, gate ring.
3. e2e S0/S1/S2, compare.md, stamp 0.16.48 / 0.17.53-v2.

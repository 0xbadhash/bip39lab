# Plan: V2 UC20 kit + ack + UC3

**Spec:** `.agents/specs/2026-08-26-v2-uc20-kit.md`

## Approach

Keep classic Lab frozen. Expand UC20 in `uc20()` with its own rail (quiz/finish inside the function, not generic `ucJob` step 2). Gate Next on correct metal (ss/ti) and solid plate only. Four-letter pad uses `BIP39Lab.generateMnemonic(12)` so words vary. Ship a local Catalyxt custody kit (hairline + panel languages, semantic strokes, photoreal ban). V2 first-visit ack reuses `.ack-overlay` tokens; `ack` sits in `bip39lab.v2` sessionStorage and Hard refresh wipes it. UC3 drops the second generate button.

## Architecture

- Kit: `web/assets/catalyxt/custody/{hairline,atom,README.md,index.html}`
- Tracks: `web/v2/js/v2-app.js`, `web/v2/css/v2.css`, `web/v2/index.html`
- Tests: `e2e/v2.spec.ts` (`enterV2` dismisses ack)

## Implementation sequence

1. UC20 lab + kit SVGs + ack overlay + UC3 generate/compare layout in v2-app/css/index.
2. Playwright V2-S0/S3 + AC stubs.
3. Release stamp product 0.16.51 / chip 0.17.75-v2.

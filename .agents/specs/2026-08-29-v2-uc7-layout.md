# V2 UC7 classroom / Try / Combine restack

- **Product:** bip39lab
- **Created:** 2026-08-29
- **Status:** ready-for-agent
- **Priority:** P0
- **Plan:** `.agents/specs/2026-08-29-v2-uc7-layout-plan.md`
- **Surface:** `web/v2/` UC7 Shamir step 0 + SLIP-39 step 1
- **Grill-me:** complete

## Problem Statement

UC7 stacked Combine next to Try so the shortcut looked like the exercise. Classroom (blue) was buried. SLIP share boxes were tall. The 2-of-3 checklist sat far from Try.

## Solution

Shamir: blue classroom → dropdown + Split → result → paste → amber **Try these M shares** → help → right-aligned **Combine any M**.

SLIP: Make practice → yellow lab → blue classroom → one-line share 1/2/3 → result → amber **Try these 2 shares** → play-in-order help → Done/To do checklist → right-aligned **Combine any 2 of 3**.

Chip `v0.17.131-v2`. Product `0.16.81` (absorbs untagged 0.16.79/80 tree). No Sign. No UC6 mix.

## Acceptance

| ID | Criterion |
|----|-----------|
| AC-1 | Shamir pad HTML order: `v2ShStory` then `v2ShMN` then `v2Sh` then `v2ShTry` then `v2ShTryHelp` then `v2ShCombine`. Try has `btn-try`. |
| AC-2 | SLIP pad: yellow Lab practice then `v2S39Story` then `v2-share-line` shares then `v2S39Try` then `v2S39TryHelp` then `v2S39Check` then `v2S39Combine`. |
| AC-3 | Combine wrappers use `v2-combine-right`. Share textareas `rows="1"` + `v2-share-line`. |
| AC-4 | No leftover `scripts/*.py` in this commit. No Sign. |

## Grill-me

**Status:** complete
**Date:** 2026-08-29

### G1 Outcome
- Q: Done?
  - A: Try is the coloured exercise next to paste; Combine is right-aligned under the help; SLIP checklist sits under Try help.

### G2 Non-goal
- Q: Kill?
  - A: Do not change combine math, Suite dock, or mix UC6.

# Plan: V2 UC6/UC7 FSM

Technical how for `.agents/specs/2026-08-24-v2-uc6-uc7-fsm.md`.

1. `web/v2/js/v2-app.js` — UC6 three `mem.cosigners`, BIP84 `exportWatchOnly` zpub; `jumpTo` + concept map; quiz why spans; `doDont` first on pads.
2. `web/v2/css/v2.css` — cosigner grid, nav-step indexes, rail-jump, Do/Do not first.
3. `e2e/v2.spec.ts` — V2-S10, S11, S12.
4. Comet S-id index + `product_plugin.yaml` v2 scenarios.
5. Classic `/` untouched except cache-bust stamp.

# V2 UC11–UC13 custody taxonomy tracks

- **Product:** bip39lab
- **Created:** 2026-08-24
- **Status:** ready-for-agent
- **Plan:** `.agents/specs/2026-08-24-v2-uc11-uc13-plan.md`
- **Priority:** P0
- **Surface:** `web/v2/` only

## Problem Statement

V2 taught seed, path, watch-only, multisig, Shamir, PSBT, xpub, and offline default, but not the custody objects people actually mix: exchange IOU vs seed you hold, hot software vs hardware signer, hot vs cold.

## Solution

Three Beginner tracks (UC11–UC13) in the existing V2 pattern: gate, Do/Do not, `desc()`, blue `done` callout, three App Shell atoms, quiz with Wrong. reasons, force exit. Classic `/` unchanged.

## Acceptance Criteria

- [ ] Picker shows 13 `.uc-card` buttons
- [ ] UC11–UC13 each mount three atoms; step 0 atom 1 is `hi`
- [ ] Teaching pads have Do/Do not + `desc()`; Quiz/Finish do not add extra desc
- [ ] Deep link `?uc=11`–`13` opens the matching gate
- [ ] V2 chip and `v2-app.js?v=` are `0.17.22-v2`
- [ ] Playwright V2-S0 count 13, V2-S13 includes 11–13, V2-S14 pads/quiz/exit
- [ ] Classic `/` still Lab (`#btnGenerate`)

## Out of Scope

Classic `/`. Lightning. Brain wallet. Paper-as-wallet. Committing leftover `scripts/*.py`.

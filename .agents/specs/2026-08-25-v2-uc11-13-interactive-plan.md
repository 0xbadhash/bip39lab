# Plan: UC11–UC13 interactive

**Spec:** `.agents/specs/2026-08-25-v2-uc11-13-interactive.md`

## Approach

`mem.tax` flags. Click labs. Lock/drain timers cleared on `startTrack`. Button colours `--ok` / `--bad`.

## Implementation

1. UC11 They/You rows, company seed-phrase + auto lock, you-hold contrast.
2. UC12 phone balance + malware drain; hardware two-pane USB/type drain; UC13 sort + trap.
3. e2e V2-S14; V2 chip `0.17.30-v2`; Comet S14 blurb.

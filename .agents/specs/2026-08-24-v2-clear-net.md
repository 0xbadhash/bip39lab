# V2 Clear secrets topbar + Test/Mainnet derive

- **Product:** bip39lab
- **Created:** 2026-08-24
- **Status:** ready-for-agent
- **Plan:** `.agents/specs/2026-08-24-v2-clear-net-plan.md`
- **Priority:** P0
- **Surface:** `web/v2/` only

## Problem Statement

Clear secrets lived only on some Generate rows. Validate & Derive always used testnet, so learners never saw bc1 versus tb1 from the same phrase.

## Solution

One red Clear secrets in the V2 topbar (right cluster) on picker, gate, and every track. Beside Validate & Derive, a Network select: Test · tb1… / Mainnet · bc1…. Changing the select re-derives if addresses are already shown.

## Acceptance Criteria

- [ ] `#v2Clear` is in `.topbar-actions`, class danger; not in `.sidebar`
- [ ] UC1 Validate shows `#v2Net` with Test/Mainnet; default test addresses `tb1`; Mainnet `bc1`
- [ ] Classic `/` unchanged (`#btnGenerate`)
- [ ] Playwright V2 suite green including V2-S0 and V2-S4

## Out of Scope

Classic entropy pad. Dice/coin UC (separate draft spec). Leftover `scripts/*.py`.

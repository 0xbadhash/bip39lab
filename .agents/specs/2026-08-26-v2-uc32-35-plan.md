# Plan: V2 UC32–UC35

**Spec:** `.agents/specs/2026-08-26-v2-uc32-35.md`

## Approach

Add four unique tracks only. Do not clone UC7, UC16, UC18, UC31, or UC5. Each track is two practice pads plus a shuffled quiz (ucJob step 2) except where the spec needs named buttons. Timelock is a finite state machine in `mem.tl`: armed, ticks, expired, refreshed — no PSBT, no Sign. SeedXOR shows two full 12-word parts and requires both; no claim of SeedXOR.com compatibility. Descriptor shows a practice `wpkh(` string. Electrum path never invents an Electrum address.

Classic Lab stays frozen except the product stamp at ship. V2 chip `0.17.89-v2` is independent. Reuse existing SVG atoms from UC6/7/8/5/16.

## Architecture

- `web/v2/js/v2-app.js` TRACKS/PATHS/GATES/steps/uc32–35/ucJob/boot `?uc=`
- `web/v2/css/v2.css` FSM + xor parts
- `web/v2/index.html` chip
- `e2e/v2.spec.ts` S0 count 35; S23–S26
- `tests/test_ac_v2_uc32_35.py`

## Implementation sequence

1. Spec already written. Wire TRACKS 32–35, gates, picker 35, `?uc=` 1–35, stepHtml/ucJob.
2. Pads: XOR parts, timelock FSM, descriptor ack, Electrum vs BIP-39; quizzes; reuse atoms.
3. Playwright S0/S23–S26, AC stubs, product 0.16.55 on ship only.

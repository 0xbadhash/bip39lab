# Plan: V2 tracks polish

**Spec:** `.agents/specs/2026-08-26-v2-tracks-polish.md`

## Approach

Keep classic Lab frozen. Bundle the operator’s V2 card fixes into one product patch: compare, mint bar, four-letter plate, quiz shuffle and language, hardware ceremony (firmware then laptop-seed-stays-hot), and path-folder teaching amounts as green chips beside addresses. Receive vs change shows both folders with different leftover vs incoming amounts. Chip `0.17.88-v2` is independent of product `0.16.54`.

This plan is the implementation sequence for that dual stamp. No new illustration family. SessionStorage still progress/ack only.

## Architecture

- `web/v2/js/v2-app.js`, `web/v2/css/v2.css`, `web/v2/index.html`
- Tests: `e2e/v2.spec.ts`

## Implementation sequence

1. UC3 live compare (no Compare, colored estimates) and UC14 mint group beside Word count.
2. UC20 full-word plate, shuffled plain-English quizzes, UC22 firmware cards + notes-file hot vault, UC4 amount chips and receive/change pair.
3. Playwright V2 suite, stamp product 0.16.54, ship chain.

# V2 UC14 extra RNG toys (+10 d6, send pad to Lab)

- **Product:** bip39lab
- **Created:** 2026-08-27
- **Updated:** 2026-08-27
- **Status:** ready-for-agent
- **Priority:** P0
- **Plan:** `.agents/specs/2026-08-27-v2-uc14-rng-toys-plan.md`
- **Surface:** `web/v2/` UC14 entropy pad only
- **Grill-me:** complete (G1–G7 from CEO lock `/tmp/w6-uc14.md`)
- **CEO lock:** WINDOW 6 UC14 leftover only. Do not reopen UC10. No UC6. No Imagine. No signer.

## Problem Statement

Classic `#cardEntPad` has two leftover toys V2 UC14 still listed as missing in `web/v2/compare.md`: **`+10 d6`** (`#btnDice10`) and **send pad to Lab** (`#btnEntToLab`). `#v2Dice10` already exists; copy must match classic and **send pad** must land practice words on the V2 First-wallet phrase (`mem.mnemonic`), because V2 has no Lab tab. Practice only. Not OS CSPRNG.

## Solution

- Keep `#v2Dice10`. Label **`+10 d6 (fast)`**. Ten simulated d6 per click (same as classic). Not a substitute for Lab Generate.
- After mint (`#v2EntMint` / `#v2EntWords`), add `#v2EntToLab` **Optional: put practice words on First wallet**. Empty pad → status, no navigation. Confirm: TEST DATA, overwrites First-wallet phrase, never fund. On accept: copy `mem.entMnemonic` → `mem.mnemonic`, clear derive cache, `setGated(1)` + `startTrack(1)` so UC1 shows those words. Do not write the mnemonic to sessionStorage.
- Classic `/` `#btnDice10` `#btnEntToLab` unchanged. No Sign. No UC6/UC10.

Chip `v0.17.116-v2`. Product stamp on ship.

## Acceptance

| ID | Criterion |
|----|-----------|
| AC-1 | UC14 `#v2Dice10` visible as `+10 d6 (fast)`. One click appends 10 `d6:` events. |
| AC-2 | After mint, `#v2EntToLab` exists. Empty words → no First-wallet jump. Confirm then copies practice words onto UC1 card. Status says practice / do not fund. |
| AC-3 | Pad toys remain simulated. Copy never funds, never signs, never writes mnemonic to sessionStorage. |
| AC-4 | Classic `#btnDice10` and `#btnEntToLab` still on `/`. V2 chip `0.17.116-v2`. |
| AC-5 | UC10 / UC6 / UC8 / Imagine / signer unchanged this ship. |

## Grill-me

**Status:** complete
**Date:** 2026-08-27

### G1 Outcome
- Q: Done in one sentence?
  - A (CEO lock): Extra RNG toys on UC14 — +10 d6 and send pad to Lab (First wallet on V2).
  - Recommended was: same.

### G2 Non-goal / kill
- Q: What must we not build?
  - A: No UC10 reopen, no UC6, no Imagine, no signer, no real dice ceremony, no CSPRNG claim.

### G3 Wrong product
- Q: Lab tab on V2?
  - A: No. Send pad → First wallet (`mem.mnemonic` / UC1). Classic Lab tab stays on `/`.

### G4 Cheapest
- Q: Smallest ship?
  - A: Label `#v2Dice10` + `#v2EntToLab` after mint. No new track.

### G5 Abuse
- Q: Failure?
  - A: Confirm overwrite. Practice-only copy. Empty pad refuses. Never persist mnemonic.

### G6 Verify
- Q: Proof?
  - A: Playwright V2-S15 still; V2-S46 send-pad + dice10 label. pytest AC file.

### G7 Priority
- Q: Why now?
  - A: WINDOW 6 leftover row 14. P0 this pane.

## Clarifications

- V2 has no `#mnemonic` Lab box. First wallet is the analog.
- `#v2Dice10` already wired; leftover was send-pad plus classic copy.

## Testing Decisions

- Red: no `#v2EntToLab`; Sign; sessionStorage mnemonic; mempool CSP change
- Green: V2-S15 dice10; V2-S46 mint then send pad → UC1 words
- pytest `tests/test_ac_v2_uc14_rng_toys.py` AC-1–5

## Out of Scope

UC10, UC6, Imagine, signer, classic Lab clone, real hardware RNG.

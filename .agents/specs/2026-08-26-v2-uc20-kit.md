# V2 UC20 materials lab, custody kit, ack overlay, UC3 compare

- **Product:** bip39lab
- **Created:** 2026-08-26
- **Status:** ready-for-agent
- **Priority:** P0
- **Plan:** `.agents/specs/2026-08-26-v2-uc20-kit-plan.md`
- **Surface:** `web/v2/` + `web/assets/catalyxt/custody/`

## Problem Statement

UC20 was a thin paper-vs-steel pick. Orientation ack lived only on classic `/`. UC3 had two generate buttons and a full-width compare dump. Track atoms used photoreal stills that broke Catalyxt UI rules.

## Solution

UC20 materials lab (paper fails, metals, 4-letter random 12-word plate, solid vs tiles vs photo with Next gated). V2 ack overlay (tracks wording). Custody kit hairline 96×48 + atom 220×88. Photoreal forbidden in V2. UC3 one Generate N-word button; compare split 50/50.

Classic `/` unchanged. Dual stamp: product `0.16.51` vs chip `0.17.75-v2`.

## Acceptance

| ID | Criterion |
|----|-----------|
| AC-1 | First `/v2/` visit shows `#v2AckOverlay`; **I understand — start the tracks** hides it |
| AC-2 | Chip `0.17.75-v2`; Hard refresh in topbar |
| AC-3 | UC3 Same words has `#v2Generate` containing N-word phrase and no `#v2Regen` |
| AC-4 | Classic `/` still `#btnGenerate` |

## Grill-me

Q: Funded phrase in sessionStorage?
A: No. Progress + ack flag only.

Q: Photoreal V2 atoms?
A: No. Custody kit SVGs. Classic 3D faces stay on `/`.

Q: Aluminium unlocks Next?
A: No. Stainless or titanium only. Solid plate only on design step.

## Testing Decisions

- V2-S0 overlay + chip
- V2-S3 one generate button
- Pytest AC stubs

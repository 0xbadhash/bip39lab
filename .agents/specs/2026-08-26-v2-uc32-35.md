# V2 UC32–UC35 unique leftover tracks

- **Product:** bip39lab
- **Created:** 2026-08-26
- **Status:** ready-for-agent
- **Priority:** P0
- **Plan:** `.agents/specs/2026-08-26-v2-uc32-35-plan.md`
- **Surface:** `web/v2/` only (classic `/` unchanged)
- **Grill-me:** complete (G1 default)

## Problem Statement

Picker is 31 tracks. Four unique jobs are still missing: SeedXOR N-of-N BIP39-looking parts (not UC7 Shamir, not UC31 SLIP-39); CSV-style inactivity timelock as an educational FSM (not UC18 packet/2-of-3); backup the output descriptor / policy string (not only keys from UC5/UC6); Electrum-style words that look like BIP-39 but BIP-39 restore is the wrong vault (not UC16 checksum drill).

## Solution

Ship **UC32–UC35**. Picker count **35**. Deep link `?uc=` 32–35. Reuse existing atoms. Practice lab: no wallet, no signer, no broadcast. Timelock is an educational state machine only — not a live CSV signer. Chip `v0.17.89-v2`. Product stamp only on ship. Classic `/` cache-bust only.

### UC32 SeedXOR N-of-N BIP39-part split
All N parts required. Each part is a full 12-word list that looks like a backup. Not threshold Shamir. Not SLIP-39. This tab teaches the job; it is not the SeedXOR product.

### UC33 On-chain timelock dead-man (edu FSM)
Owner arms a timer. Heir cannot spend until simulated time expires. Owner refresh resets the timer. No PSBT sign. No broadcast. Not legal counsel. Distinct from UC18 sealed packet / 2-of-3.

### UC34 Descriptor / policy backup
Recovery can need the policy string (wpkh / wsh / sortedmulti), not only the words. Show a practice descriptor. Ack: save the policy with the keys. Distinct from UC5 export and UC6 three seeds.

### UC35 Non-BIP39 Electrum seed recovery
Twelve English words can still be Electrum’s stretch, not BIP-39. BIP-39 restore opens the wrong vault (or empty). This tab does not run Electrum KDF. Distinct from UC16.

## Acceptance

| ID | Criterion |
|----|-----------|
| AC-1 | `#pickerGrid` with `[data-path-filter="all"]` shows **35** `.uc-card`. Chip `0.17.89-v2`. `?uc=32` through `?uc=35` open gates. |
| AC-2 | UC32: `#v2XorNeedAll` after showing both parts; quiz: all parts required, not 2-of-3 Shamir. |
| AC-3 | UC33: `#v2TlHeir` fails before timeout; succeeds only after simulated expiry; `#v2TlRefresh` resets; no Sign button. |
| AC-4 | UC34: `#v2DescLine` contains `wpkh` or `wsh`; `#v2DescAck` unlocks Continue. |
| AC-5 | UC35: `#v2ElBip39` shows a tb1 practice address; `#v2ElElectrum` states BIP-39 restore is the wrong vault; no Electrum address invented. |
| AC-6 | Classic `/` still `#btnGenerate`. No new photoreal atoms. |

## Grill-me

Q: Does SeedXOR in this tab produce a funded-compatible XOR product backup?
A: No. Classroom N-of-N: every part looks like 12 words. Not Shamir. Not SLIP-39.

Q: Does UC33 sign or broadcast a CSV timelock?
A: No. Educational FSM only. Heir spend is a state label, not a transaction.

Q: Does UC35 compute an Electrum address?
A: No. It refuses to fake Electrum KDF. It only shows that BIP-39 rules are the wrong restore.

Q: sessionStorage mnemonic?
A: No. Progress + ack + optional dock only.

## Testing Decisions

- Red: V2-S0 `.uc-card` count still 31 before implement
- Green: `npx playwright test e2e/v2.spec.ts` — S0 count 35 + chip; S23 UC32; S24 UC33; S25 UC34; S26 UC35
- pytest `tests/test_ac_v2_uc32_35.py` AC-1…AC-6 stubs
